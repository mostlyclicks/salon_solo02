<%= render 'refinery/admin/search_header', :url => refinery.promos_admin_promos_path %>
<div class='pagination_container'>
  <% if @promos.any? %>
    <%= render 'promos' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
